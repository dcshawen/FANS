name: FANS-Dev

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: FANS-React-Dev
    ports:
      - "5173:5173"
    volumes:
      - /${PWD}/frontend:/app
      - /app/node_modules
